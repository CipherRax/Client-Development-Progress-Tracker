export const APP_NAME = 'Trackly';

/**
 * Single source of truth for the product version shown in the admin footer's
 * "build" tag. Keep in sync with web/package.json.
 */
export const APP_VERSION = '1.0.0';

/**
 * Region tag rendered in the footer. Placeholder wording until a registered
 * company address exists — to change where we say this product is built,
 * edit this one constant and it updates across the whole app.
 */
export const FOOTER_REGION_TAG = 'Built in Africa';